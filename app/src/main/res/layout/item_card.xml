<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="16dp"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Front text -->
        <TextView
            android:id="@+id/tvFront"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:textAppearance="@style/TextAppearance.Material3.TitleMedium"
            app:layout_constraintEnd_toStartOf="@id/actionRow"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Front image (always visible if present) -->
        <ImageView
            android:id="@+id/imgFront"
            android:layout_width="0dp"
            android:layout_height="140dp"
            android:layout_marginTop="8dp"
            android:contentDescription="Front image"
            android:scaleType="centerCrop"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="@id/tvFront"
            app:layout_constraintStart_toStartOf="@id/tvFront"
            app:layout_constraintTop_toBottomOf="@id/tvFront" />

        <!-- Hint (tap to show/hide) -->
        <TextView
            android:id="@+id/tvHint"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textAppearance="@style/TextAppearance.Material3.BodySmall"
            app:layout_constraintEnd_toEndOf="@id/tvFront"
            app:layout_constraintStart_toStartOf="@id/tvFront"
            app:layout_constraintTop_toBottomOf="@id/imgFront" />

        <!-- Divider (only visible when answer is shown) -->
        <View
            android:id="@+id/divider"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_marginTop="10dp"
            android:background="?attr/colorOutline"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="@id/tvFront"
            app:layout_constraintStart_toStartOf="@id/tvFront"
            app:layout_constraintTop_toBottomOf="@id/tvHint" />

        <!-- Back text (only shown when expanded) -->
        <TextView
            android:id="@+id/tvBack"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textAppearance="@style/TextAppearance.Material3.BodyLarge"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="@id/tvFront"
            app:layout_constraintStart_toStartOf="@id/tvFront"
            app:layout_constraintTop_toBottomOf="@id/divider" />

        <!-- Back image (only visible when expanded and image exists) -->
        <ImageView
            android:id="@+id/imgBack"
            android:layout_width="0dp"
            android:layout_height="140dp"
            android:layout_marginTop="8dp"
            android:contentDescription="Back image"
            android:scaleType="centerCrop"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="@id/tvFront"
            app:layout_constraintStart_toStartOf="@id/tvFront"
            app:layout_constraintTop_toBottomOf="@id/tvBack" />

        <!-- Actions (Edit / Delete buttons) -->
        <LinearLayout
            android:id="@+id/actionRow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnEdit"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:contentDescription="Edit card"
                android:text=""
                app:icon="@android:drawable/ic_menu_edit"
                app:iconSize="22dp"
                app:iconTint="?attr/colorOnSurface" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnDelete"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:contentDescription="Delete card"
                android:text=""
                app:icon="@android:drawable/ic_menu_delete"
                app:iconSize="22dp"
                app:iconTint="?attr/colorError" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>